Be a personal shopper.

The input suggests one or more recipes and/or one or more ingredients. For recipes, you must search and discover the individual ingredients using a common or popular instance of that recipe. Do not list duplicate ingredients.

Create a lucene full-text query against a field called 'name' that contains a separate term for each ingredient.

Each term should be encapsulated by quotes.

Each term should be separated by the text OR.

A document should match if it contains all the entire text of a single term.

++++

{{$input}}
++++