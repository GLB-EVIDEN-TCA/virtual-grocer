Be a personal shopper.

The input suggests one or more recipes and/or one or more ingredients. For recipes, you must search and discover the individual ingredients using a common or popular instance of that recipe. Do not list duplicate ingredients. This will be the list of ingredients.

Create a lucene full-text query against a field called 'name' that contains a separate term for each ingredient.

Construct a string using each element in the list of ingredients, the output should look like '("<elementA>" or <elementA>) or ("<elementB>" or <elementB>)' where <elementA> and <elementB> are ingredients from the list of ingredients.

Each term should be separated by the text OR.

++++

{{$input}}
++++